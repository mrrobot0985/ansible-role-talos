---
title: "Talos Cluster Documentation"
description: "Live-generated documentation for {{ cluster_name }}"
---

Welcome to the live-generated documentation for your Talos Linux cluster.

---

## Overview

| Property       | Value |
|----------------|-------|
| Cluster Name   | `{{ cluster_name }}` |
| Talos Version  | `{{ talos_facts.version | default('unknown') }}` |
| Node Count     | {{ groups.all | length }} |
| Control Planes | {{ groups.talos_controlplane | default([]) | length }} |
| Workers        | {{ groups.talos_workers | default([]) | length }} |
| CNI            | `{{ cni | default('unknown') | capitalize }}` |
{% if talos_vip | default('', true) | length > 0 %}
| VIP            | `{{ talos_vip }}` |
{% endif %}

---

## Quick Links

- [Cluster Overview](cluster.md)
{% for host in groups.all | sort %}
- [{{ host }}](nodes/{{ host }}.md)
{% endfor %}

---

## Structured Data

Raw YAML data files (NetBox DCIM-compatible schema) are available under `.generated/data/`:

- `.generated/data/cluster.yml` — Cluster-wide metadata
{% for host in groups.all | sort %}
- `.generated/data/nodes/{{ host }}.yml`
{% endfor %}

---

*Generated by the mrrobot0985.talos Ansible role from live node facts.*
